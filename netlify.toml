# This is the redirect rule for a Single-Page Application (SPA).
# It ensures that all paths are served by your index.html file,
# allowing client-side routing to work correctly.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200